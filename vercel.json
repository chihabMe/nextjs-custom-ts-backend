{
          "version": 2,
            "builds": [
              {
                "src": "./server/app.ts",   // server.ts is in root
                "use": "@vercel/node"
              }
            ],
            "routes": [
              {
                "src": "/(.*)",
                "dest": "server/app.ts"
              }
            ]
          
